[pytest]
addopts = -m "not download"
# Download-marked tests represent fallback flows when streaming is impossible.
markers =
    online: marks tests that require cubo/network access (deselect with '-m "not online"')
    streaming: marks tests that exercise streaming-first code paths
    download: marks tests that require full dataset downloads and are skipped by default
pythonpath = code . src
